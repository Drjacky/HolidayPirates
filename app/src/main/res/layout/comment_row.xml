<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rlvCommentRow"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:focusable="true"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:clickable="true"
    android:background="?android:attr/selectableItemBackground"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/comment_row_imgAvatar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/default_avatar"
        android:layout_alignParentLeft="true"
        android:layout_margin="10dp"/>

    <TextView
        android:id="@+id/comment_row_txtCommentName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        android:textSize="@dimen/comment_row_name"
        android:textStyle="bold"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@id/comment_row_imgAvatar"
        android:maxLines="1"
        android:maxLength="20"
        android:ellipsize="end"/>

    <TextView
        android:id="@+id/comment_row_txtCommentEmail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/gray"
        android:textSize="@dimen/comment_row_email"
        android:layout_below="@id/comment_row_txtCommentName"
        android:layout_alignParentRight="true"
        android:layout_toRightOf="@id/comment_row_imgAvatar"
/>

    <TextView
        android:id="@+id/comment_row_txtCommentBody"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/comment_row_imgAvatar"
        android:maxLines="2"
        android:maxLength="40"
        android:ellipsize="end"/>



</RelativeLayout>